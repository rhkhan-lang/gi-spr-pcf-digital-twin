# Data folder

This folder contains the browser-demo surrogate files, manuscript reference values, and templates for replacing the demonstration model with a FEM-trained/validated surrogate.

Important: files marked `demo/synthetic placeholder` are not publication-grade FEM validation data. Replace them with real COMSOL/FEM results before claiming digital-twin prediction accuracy.
